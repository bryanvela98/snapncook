# Bootstrap inputs. account_id is not a secret (it appears in every ARN) but is
# kept out of source defaults so the module stays account-agnostic.
account_id   = "945504685682"
aws_region   = "us-east-1"
project_name = "snap-and-cook"
